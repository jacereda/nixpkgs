--- a/temperature/temperature
+++ b/temperature/temperature
@@ -46,9 +46,8 @@
 # Get chip temperature
 open (SENSORS, "sensors -u $chip |") or die;
 while (<SENSORS>) {
-    if (/^\s+temp1_input:\s+[\+]*([\-]*\d+\.\d)/) {
-        $temperature = $1;
-        last;
+    if (/^\s+temp.*_input:\s+[\+]*([\-]*\d+\.\d)/) {
+        $temperature = $1 if $1 > $temperature;
     }
 }
 close(SENSORS);
